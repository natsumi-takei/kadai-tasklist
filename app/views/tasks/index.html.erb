<% if logged_in? %>
  <%= render 'layouts/tasks',tasks: @tasks %>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the TaskLists !</h1>
      <%= link_to '新規登録', signup_path, class:'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>
